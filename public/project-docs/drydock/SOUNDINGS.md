# Soundings

| ID | Acceptance Criterion | State | Evidence |
|---|---|---|---|
| CLI-001 | `drydock --help` shows the complete public command surface | DONE | `test_cli.py::TestHelpAndVersion` |
| CLI-002 | `drydock --version` shows version and copyright | DONE | `test_cli.py::TestHelpAndVersion` |
| CLI-003 | `drydock config show` displays effective configuration values and sources (7 rows: build_directory, workspace, drydock_model, llm_provider, prompt_warn_kb, quarterdeck_port, shipslog_dir) | DONE | `test_cli.py::TestConfigShow`, `test_config.py::TestConfigShow::test_show_returns_seven_rows` |
| CLI-004 | `drydock config set <key> <value>` persists configuration; keys include drydock_workspace, drydock_build_directory, drydock_model, llm_provider, prompt_warn_kb, quarterdeck_port, shipslog_dir | DONE | `test_cli.py::TestConfigSet`, `test_config.py::TestConfigSet`, `test_config.py::TestGetModel` |
| CLI-005 | Targets resolve to `$DRYDOCK_WORKSPACE/targets/<Target>` (git-root/cwd default); a Target's Blueprint is its `blueprint/` subtree | DONE | `test_config.py::TestWorkspaceResolution` |
| CLI-006 | `drydock config set llm_provider <claude\|codex>` persists a valid subscription CLI provider | DONE | `test_cli.py::TestConfigSet`, `test_config.py::TestConfigSet` |
| CLI-007 | `drydock config set prompt_warn_kb <kb>` persists a valid prompt-size threshold | DONE | `test_cli.py::TestConfigSet`, `test_config.py::TestConfigSet` |
| CLI-008 | `drydock config set quarterdeck_port <port>` persists a valid QuarterDeck port | DONE | `test_config.py` quarterdeck-port tests, `test_cli.py::TestRunQuarterdeck::test_run_quarterdeck_config_port_used` |
| CLI-009 | `drydock init <Target>` creates the minimal Target scaffold (`METADATA.md` with name/display_name/short_description/stack/version fields, root Sea Trials/Soundings, `blueprint/sources/`, state-only QuarterDeck) | DONE | `test_cli.py::TestInit`, `test_init_target.py`, `test_metadata.py` |
| CLI-010 | `drydock run quarterdeck [<Target>] [--host HOST] [--port PORT]` serves the package runtime against a named or sole-Target in-tree console state | DONE | `test_cli.py::TestRunQuarterdeck`, `test_quarterdeck_run.py` |
| CLI-011 | `drydock status <Blueprint> <Target>` validates a Target's Blueprint completeness and conventions | DONE | `test_cli.py::TestValidate`, `test_validate_specification.py` |
| CLI-012 | `drydock status <Blueprint> <Target> --verbose` shows passing checks and findings | DONE | `test_cli.py::TestValidate::test_validate_verbose_shows_passes` |
| CLI-013 | `drydock rigging compact <Blueprint> <Target> [--all] [--force]` refreshes stale compact derivatives with execution evidence | DONE | `test_cli.py::TestRiggingCompact`, `test_rigging_compact.py` |
| CLI-014 | `drydock document generate <Target> [--model <model>]` creates Target build `docs/DOC-*.md` summaries from the Target Blueprint and `MANIFEST.md` through the Drydock LLM adapter; configuration lives at the Target root; strict delimited artifact validation rejects noisy, duplicate, unexpected, malformed, or partial LLM output before writing generated documentation files | IMPLEMENTED | `test_target_documentation.py::test_generate_documentation_writes_doc_files_and_config`, bad-output/no-write and missing-target tests |
| CLI-015 | `drydock document assemble <Target> [--theme <theme>]` renders Target build `DOC-*.md` documentation into `$DRYDOCK_BUILD_DIRECTORY/<Target>/docs/index.html` with Prototyper-style sidebar navigation, configured section order, and selectable theme CSS | IMPLEMENTED | `test_cli.py::TestDocumentAssemble`, `test_target_documentation.py::test_assemble_documentation_writes_sidebar_app` |
| CLI-016 | `drydock document <Target> [--model <model>] [--theme <theme>]` runs the full Target documentation pipeline | IMPLEMENTED | `test_target_documentation.py::test_document_target_runs_generate_then_assemble` |
| CLI-017 | `drydock rigging update <Target>` injects `BUSINESS_RULES_compact.md` into `AGENTS.md` via `DRYDOCK_RIGGING_START/END` markers and stamps provenance fields into `METADATA.md`; `--dry-run` previews without writing | IMPLEMENTED | `test_rigging_update_verify.py::TestUpdate`, `test_rigging_update_verify.py::TestRiggingUpdateCLI` |
| CLI-018 | `drydock rigging verify <Target>` checks injected block currency (content hash), required human fields (`name`, `short_description`), and stamp fields; exits 0/1 | IMPLEMENTED | `test_rigging_update_verify.py::TestVerify`, `test_rigging_update_verify.py::TestRiggingVerifyCLI` |
| CLI-019 | `drydock plan create <Target>` authors typed Blueprint spec files from the reviewed analysis (single LLM call, strict delimited artifact contract), emits `MANIFEST.md` with state-preserving merge on replan (restores `applied_specs` verbatim, carries `closed/verified` block states for clean files, resets dirty files to `pending`, protects applied Blueprint files from overwrite), injects the persistent `PLAN_COMPASS.md` standing directive, validates the complete LLM response before target writes, rejects blocked/error/malformed/partial output without writing Blueprint, Manifest, or QuarterDeck artifacts, runs a deterministic integrity gate (depends/acyclic/implements/~100-cap, all fatal), writes the Planning Session only after validation passes, adopts already-typed spec files found under `blueprint/sources/` directly into `blueprint/` before planning, and selects a dedicated `plan_create_speckit` prompt (Spec Kit Import Contract mapping plus a `CONVERSION_REPORT.md` review-evidence block) when `blueprint/sources/.specify/` is present from a prior `import --format speckit` | IMPLEMENTED | `test_planning_session.py` (replan_preserves_closed_verified, replan_resets_dirty, replan_restores_applied_specs, replan_preserves_spike_finding, load_prior_plan_state, spec_is_dirty, test_speckit_source_selects_speckit_prompt), `test_cli.py::TestPlanningSession`, `test_standard_artifacts.py` |
| CLI-020 | `drydock build status <Blueprint> <Target>` reports build progress (plan vs. implementation) grouped by feature, with per-feature and overall rollups and the buildable frontier (`buildable_steps()`) | DONE | `test_build_status.py` (grouping, ac folding, ungrouped, buildable marks, rollups), `test_cli.py::TestPlanInspection::test_build_status_reports_grouped_progress_and_frontier` |
| CLI-021 | `drydock build <Target> [--step <step-id>] [--force]` builds the buildable frontier or one selected step, runs Blueprint `Programmatic Acceptance` Python assertions after each successful story build, writes evidence, and transitions block state; forced selected builds reset the step and child ACs to pending; guards on dirty Drydock source and stack dirs; creates the configured build root on first use; updates applied registry with HEAD commit per step; records applied Blueprint spec hashes in `applied_specs`; blocks when a previously applied Blueprint spec changes or is deleted; uses compact stack files for files already in the registry | IMPLEMENTED | `test_build_run.py` (frontier order, selected step, force rebuild reset, programmatic acceptance pass/fail, legacy AC auto-verify, failure stop, evidence, allow_tools, dirty guards, applied registry, applied spec provenance, stale/deleted applied specs), `test_build_plan.py::TestAppliedSpecs`, `test_config.py::TestWorkspaceResolution::test_build_directory_env_is_created_when_missing`, `test_cli.py::TestPlanInspection::test_build_executes_buildable_frontier`, `test_cli.py::TestPlanInspection::test_build_step_force_rebuilds_selected_step` |
| CLI-021A | `drydock build verify <Target> <step-id>` remains a legacy compatibility command for previously implemented story/spike states; new build flow closes stories from automatic programmatic acceptance | IMPLEMENTED | `test_build_review.py`, `test_cli.py::TestPlanInspection::test_build_verify_marks_step_and_acs_verified` |
| CLI-022 | `drydock build score <Blueprint> <Target>` generates `SCORECARD.md` | STUBBED | `test_cli.py::TestStubs` |
| CLI-023 | `drydock refit <Target>` conforms change tickets in `blueprint/changes/` to the build process, normalizes headers, and patches MANIFEST.md | IMPLEMENTED | `tests/test_refit.py`, `tests/test_cli.py::TestRefit` |
| CLI-024 | `drydock analyze <Target> [--model <model>]` runs Scrum-team Blueprint analysis; emits quality signal (Blocked/Questions/Ready), feature-grouped story list, ANALYSIS.md, SEA_TRIALS.md, SOUNDINGS.md, COMPASS.md (if absent/unpopulated), fixed spike-*.json questionnaires (intent/stack/guardrails — Ownership-test gated), optional variable spikes; validates strict delimited artifact output and rejects prose/tool transcripts/malformed output before writing generated artifacts; normalizes identity questionnaire answers from generated proposals and stack questionnaires as alphabetized `checkbox_grid` options; suppresses duplicate Open Questions content in ANALYSIS.md because questionnaire action items are the question surface; injects the persistent `ANALYZE_COMPASS.md` standing directive and the Rigging catalog filename list (BRA*.md + stack/*.md, no README.md); advances METADATA.md lifecycle state to "analyzed" (forward-only) and fills commanders_chair.html with Features, Stories, Questionnaires, and Blockers when state advances; read-only w.r.t. blueprint/ and MANIFEST.md; prompt assembly order is job-block → inputs → body (instructions last); model resolves via --model → DRYDOCK_MODEL → sonnet | IMPLEMENTED | `test_analyze.py` — unit, CLI contract, strict artifact validation/no-write, file-write, lifecycle, compass-detection, commanders chair, duplicate-question suppression, Story List parser regression, questionnaire normalization; `test_prompts.py::TestInputTokens::test_analyze_prompt_keeps_questions_out_of_analysis_markdown`; `test_metadata.py` lifecycle state; `test_manifest.py` node graph model |
| CLI-025 | `drydock import <Target> <Source> --format <auto\|markdown\|source\|speckit\|compass>` preserves an explicit Markdown file or complete directory tree under `blueprint/sources/`; `--format auto` accepts files only; `--format compass` accepts files only and copies the source to `COMPASS.md`; `intent` remains a compatibility alias for `compass`; `--format source` imports source trees; `--format speckit` translates Spec Kit projects to a Blueprint with conversion report | DONE | `test_import_markdown.py`, `test_import_source.py`, `test_import_speckit.py`, `test_cli.py::TestImport`, `test_cli.py::TestPlanningSession::test_markdown_import_plan_create_and_approve`, `test_import_intent.py` |
| CLI-026 | `drydock status <Blueprint> <Target>` reports plan state and runnable frontier | DONE | `test_cli.py::TestStatus::test_status_blueprint_target_reports_plan_state`, `test_status.py::TestStatusBlueprintTarget` |
| CLI-027 | `drydock status <Blueprint>` reports Blueprint validation summary | DONE | `test_cli.py::TestStatus::test_status_blueprint_reports_validation_summary`, `test_status.py::TestStatusBlueprint` |
| CLI-028 | `drydock status` (no args) shows compact dashboard of last active project and records activity | DONE | `test_cli.py::TestStatus::test_status_no_args_*`, `test_status.py::TestStatusCurrent` |
| CLI-029 | `drydock survey <Target> [--run] [--import <path>] [--command <name>] [--raw]` scores each command against `<Target>/survey/ac/` acceptance criteria, appends `<Target>/survey/scores.jsonl`, and renders the scoreboard; deterministic scoring math with an injected LLM runner; survey import validates strict `SURVEY-*.md` artifact blocks and rejects noisy or unexpected LLM output before writing AC files | IMPLEMENTED | `tests/test_survey.py`, `src/drydock/survey.py` |
| CLI-030 | `drydock prompt review <component>` evaluates a Drydock prompt against the authoritative spec slice, matching notes file, and consumer contracts; computes deterministic weighted `x/10` scorecard; writes current review plus archived prior review under `docs/prompt_reviews/` | IMPLEMENTED | `tests/test_prompt_review.py`, `tests/test_cli.py::TestPromptReview`, `src/drydock/prompt_review.py` |
| CLI-031 | `drydock publish <Source.md> --output <Output.html> [--theme <theme>] [--pdf] [--pdf-output <Output.pdf>]` deterministically renders frontmatter Markdown to publishable HTML and optionally PDF without LLM execution | IMPLEMENTED | `tests/test_build_documentation.py`, `tests/test_cli.py::TestPublish` |
| CLI-032 | `drydock shipslog [--dir <path>] [--dry-run]` generates one development-log post per fully elapsed aligned Thursday-to-Wednesday week with unpublished Ship's Log events; the week in progress is never published; empty weeks are skipped; posts package resolves from --dir, then shipslog_dir config, then ./ShipsLog | IMPLEMENTED | `tests/test_shipslog.py` (week alignment, completeness gate, cursor, dry-run, failure stop, package resolution), `tests/test_cli.py::TestHelpAndVersion` |
| QD-001 | Commanders Chair, Sea Trials, and Soundings are standard pinned Core artifacts | DONE | `QuarterDeck/console.yaml`, `tests/test_quarterdeck.py::test_drydock_console_pins_the_three_standard_artifacts_in_core` |
| QD-002 | Core artifacts appear in the order Commanders Chair, Master Blueprint, Sea Trials, Soundings, Ship's Log | DONE | `QuarterDeck/console.yaml`, `tests/test_quarterdeck.py::test_drydock_console_core_artifact_order` |
| QD-003 | Soundings is a single acceptance ledger and QuarterDeck calculates acceptance totals | DONE | `QuarterDeck/app.py::render_command_status`, `tests/test_quarterdeck.py` |
| QD-004 | `drydock init` creates target-local Sea Trials and Soundings without overwriting existing files | DONE | `src/drydock/init_target.py`, `tests/test_init_target.py` |
| QD-005 | `drydock plan create` preserves standard artifacts and projects plan acceptance gates into Soundings | DONE | `src/drydock/planning_session.py`, `tests/test_standard_artifacts.py` |
| QD-006 | QuarterDeck YAML configuration drives phase-based target navigation and item rendering | DONE | `QuarterDeck/app.py`, `src/drydock/standard_artifacts.py`, `tests/test_quarterdeck.py`, `tests/test_standard_artifacts.py` |
| QD-007 | QuarterDeck document items collapse Markdown, HTML, and PDF variants into one tabbed view | DONE | `QuarterDeck/app.py::render_document_item`, `tests/test_quarterdeck.py` |
| QD-008 | QuarterDeck source rules auto-discover files while explicit items and overrides take priority | DONE | `QuarterDeck/app.py::_expand_sources`, `tests/test_quarterdeck.py` |
| QD-009 | QuarterDeck navigation has no archive section or archive controls | DONE | `QuarterDeck/app.py`, `QuarterDeck/console.yaml`, `src/drydock/standard_artifacts.py`, `tests/test_quarterdeck.py`, `tests/test_standard_artifacts.py` |
| QD-010 | Planning Session review renders the Manifest and build frontier; runnable work is determined by block state and dependencies, not whole-plan approval | IMPLEMENTED | `tests/test_build_plan.py` |
| QD-011 | A Markdown item with `tabs: true` renders its `## ` sections as switchable tabs; the init console declares ANALYSIS as a file-existence-gated tabbed Core artifact | DONE | `QuarterDeck/app.py::render_markdown_item`, `src/drydock/standard_artifacts.py`, `tests/test_quarterdeck.py::test_markdown_renderer_tabs_splits_h2_sections`, `tests/test_standard_artifacts.py::test_render_console_declares_tabbed_analysis_item` |
| QD-012 | Questionnaires (spike and non-spike) render buttonless and autosave answers on field blur; no resolution buttons, only answered fields feed downstream | DONE | `QuarterDeck/app.py::render_questionnaire`, `tests/test_quarterdeck.py::test_spike_questionnaire_is_buttonless_with_autosave`, `::test_spike_questionnaire_done_still_renders_editable_form`, `::test_non_spike_questionnaire_is_also_buttonless`, `::test_answered_discovery_stays_in_analyze_section` |
| QD-013 | QuarterDeck `compass` type renders `BUILD_COMPASS.md` as one navigable pane with live story-point rollups (`bytes/4`, derived, never persisted) and a total; move/combine/split/rename each persist the clean authored file immediately and re-render; an absent file seeds a flat grouping from the Manifest spec set | IMPLEMENTED | `src/drydock/build_compass.py`, `QuarterDeck/app.py::render_compass`, `tests/test_build_compass.py`, `tests/test_quarterdeck_compass.py` |
| GOV-001 | The Drydock specification is the sole approved behavior authority | DONE | `AGENTS.md`, `CONTRIBUTING.md` |
| GOV-002 | Completed capabilities update Soundings state and evidence | DONE | `AGENTS.md` |
| GOV-003 | Material decisions and milestones are recorded and audited through the Ship's Log workflow | DONE | `AGENTS.md`, `bin/ships_log.py`, `tests/test_ships_log_tool.py` |
