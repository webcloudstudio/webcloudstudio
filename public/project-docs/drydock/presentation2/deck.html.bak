<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Drydock — Live Walkthrough</title>
  <style>
    :root {
      --deep: #06120d;
      --navy: #071b2b;
      --paper: #f4efe2;
      --muted: #a8bbb1;
      --green: #2cb67d;
      --green-dark: #0a5c38;
      --gold: #d4a017;
      --blue: #3b5fc0;
      --purple: #8b5cf6;
      --red: #fb7185;
    }

    * { box-sizing: border-box; }
    html, body { height: 100%; margin: 0; }
    body {
      overflow: hidden;
      color: var(--paper);
      background: var(--deep);
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    .slide {
      position: fixed;
      inset: 0;
      display: none;
      padding: 7vh 8vw;
      background:
        radial-gradient(circle at 82% 14%, rgba(44, 182, 125, .14), transparent 34%),
        radial-gradient(circle at 9% 96%, rgba(59, 95, 192, .20), transparent 40%),
        linear-gradient(145deg, var(--deep), var(--navy));
    }
    .slide.active { display: grid; animation: enter .32s ease-out; }
    .slide.center { place-content: center; text-align: center; }
    @keyframes enter {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .eyebrow {
      margin-bottom: 2vh;
      color: var(--green);
      font-size: 1.8vh;
      font-weight: 800;
      letter-spacing: .28em;
      text-transform: uppercase;
    }
    h1, h2, h3, p { margin: 0; }
    h1 { font-size: clamp(3.4rem, 8vh, 7rem); line-height: .98; letter-spacing: -.045em; }
    h2 { font-size: clamp(2.8rem, 6vh, 5.4rem); line-height: 1; letter-spacing: -.035em; }
    .lead { margin-top: 2.3vh; color: #dce7e1; font-size: 2.7vh; line-height: 1.35; }
    .muted { color: var(--muted); }
    .accent { color: var(--gold); }

    .brand {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 4vh;
    }
    .brand img {
      width: min(19vh, 180px);
      filter: drop-shadow(0 16px 30px rgba(0, 0, 0, .35));
    }
    .brand-copy { text-align: left; }

    .sail-route {
      position: relative;
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      width: min(82vw, 1460px);
      margin: 6vh auto 0;
    }
    .sail-route::before {
      content: "";
      position: absolute;
      z-index: 0;
      top: 4.2vh;
      left: 12.5%;
      right: 12.5%;
      height: 3px;
      background: linear-gradient(90deg, var(--gold), var(--green), var(--blue), var(--purple));
    }
    .sail-stop {
      position: relative;
      z-index: 1;
      text-align: center;
    }
    .sail-letter {
      width: 8.4vh;
      height: 8.4vh;
      display: grid;
      place-items: center;
      margin: 0 auto 1.5vh;
      border: 3px solid var(--green);
      border-radius: 50%;
      color: var(--paper);
      background: var(--deep);
      font-size: 3.6vh;
      font-weight: 900;
      box-shadow: 0 0 0 8px rgba(44, 182, 125, .08);
    }
    .sail-stop:nth-child(1) .sail-letter { border-color: var(--gold); }
    .sail-stop:nth-child(3) .sail-letter { border-color: var(--blue); }
    .sail-stop:nth-child(4) .sail-letter { border-color: var(--purple); }
    .sail-stop h3 { font-size: 2.35vh; }
    .sail-stop p {
      margin-top: .8vh;
      color: var(--muted);
      font-size: 1.55vh;
      line-height: 1.4;
    }
    .practices {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 2.5vw;
      margin-top: 5vh;
      color: var(--paper);
      font-size: 2.6vh;
      font-weight: 800;
    }
    .practices .plus { color: var(--gold); font-size: 3.2vh; }

    .feature-stage {
      display: grid;
      grid-template-rows: auto 1fr;
      height: 100%;
    }
    .feature-head {
      display: flex;
      justify-content: space-between;
      align-items: start;
      gap: 3vw;
    }
    .feature-head .practices {
      justify-content: flex-end;
      margin: 2vh 0 0;
      font-size: 2vh;
      white-space: nowrap;
    }
    .constellation {
      position: relative;
      width: min(80vw, 1380px);
      height: 56vh;
      margin: 2vh auto 0;
    }
    .constellation::before,
    .constellation::after {
      content: "";
      position: absolute;
      top: 50%;
      left: 50%;
      border: 1px solid rgba(44, 182, 125, .2);
      border-radius: 50%;
      transform: translate(-50%, -50%);
    }
    .constellation::before { width: 62%; height: 78%; }
    .constellation::after { width: 88%; height: 48%; border-color: rgba(212, 160, 23, .18); }
    .feature-core {
      position: absolute;
      z-index: 2;
      top: 50%;
      left: 50%;
      width: 23vh;
      height: 23vh;
      display: grid;
      place-content: center;
      border: 2px solid var(--green);
      border-radius: 50%;
      background: radial-gradient(circle, rgba(10, 92, 56, .62), rgba(6, 18, 13, .96));
      box-shadow: 0 0 60px rgba(44, 182, 125, .18);
      transform: translate(-50%, -50%);
      text-align: center;
    }
    .feature-core strong { color: var(--green); font-size: 2.55vh; line-height: 1.1; text-transform: uppercase; }
    .feature-core span { margin-top: .8vh; color: var(--paper); font-size: 2.05vh; font-weight: 800; }
    .feature-hook {
      position: absolute;
      z-index: 3;
      color: var(--paper);
      font-size: 2.35vh;
      font-weight: 800;
      letter-spacing: -.01em;
      white-space: nowrap;
    }
    .feature-hook::before { content: "•"; margin-right: .7vw; color: var(--gold); }
    .hook-best { top: 7%; left: 6%; }
    .hook-simple { top: 7%; right: 6%; }
    .hook-graph { top: 33%; left: 0; }
    .hook-change { top: 33%; right: 0; }
    .hook-observe { bottom: 28%; left: 8%; }
    .hook-govern { right: 8%; bottom: 28%; }

    .agenda {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2.2vw;
      margin-top: 6vh;
    }
    .agenda article {
      min-height: 31vh;
      padding: 3.2vh 2.2vw;
      border: 1px solid rgba(255, 255, 255, .12);
      border-radius: 22px;
      background: rgba(255, 255, 255, .05);
    }
    .agenda .number {
      width: 5vh;
      height: 5vh;
      display: grid;
      place-items: center;
      margin-bottom: 2.2vh;
      border-radius: 50%;
      color: #06120d;
      background: var(--green);
      font-size: 2.2vh;
      font-weight: 900;
    }
    .agenda h3 { font-size: 3.2vh; }
    .agenda ul { margin: 2.2vh 0 0; padding: 0; list-style: none; }
    .agenda li {
      margin-top: 1.3vh;
      color: var(--muted);
      font-size: 2.05vh;
      line-height: 1.35;
    }
    .agenda li::before { content: "› "; color: var(--green); font-weight: 900; }

    .process-flow {
      position: relative;
      display: grid;
      grid-template-columns: repeat(6, 1fr);
      width: min(85vw, 1540px);
      margin: 8vh auto 0;
    }
    .process-flow::before {
      content: "";
      position: absolute;
      z-index: 0;
      top: 3.1vh;
      left: 8.33%;
      right: 8.33%;
      height: 2px;
      background: linear-gradient(90deg, var(--gold), var(--green), var(--blue), var(--purple));
    }
    .process-step {
      position: relative;
      z-index: 1;
      text-align: center;
    }
    .process-marker {
      width: 6.2vh;
      height: 6.2vh;
      display: grid;
      place-items: center;
      margin: 0 auto 2vh;
      border: 2px solid var(--green);
      border-radius: 50%;
      color: var(--deep);
      background: var(--green);
      font-size: 2vh;
      font-weight: 900;
      box-shadow: 0 0 0 7px rgba(44, 182, 125, .08);
    }
    .process-step:first-child .process-marker { border-color: var(--gold); background: var(--gold); }
    .process-step:nth-child(5) .process-marker { border-color: var(--blue); background: var(--blue); color: var(--paper); }
    .process-step:nth-child(6) .process-marker { border-color: var(--purple); background: var(--purple); color: var(--paper); }
    .process-step h3 {
      max-width: 15ch;
      margin: 0 auto;
      font-size: 2.2vh;
      line-height: 1.18;
    }
    .process-step p {
      max-width: 19ch;
      margin: 1vh auto 0;
      color: var(--muted);
      font-size: 1.55vh;
      line-height: 1.35;
    }
    .planning-contract {
      display: grid;
      gap: .7vh;
      width: 17vw;
      margin: 2.2vh auto 0;
      color: var(--paper);
      font-size: 1.55vh;
      font-weight: 750;
      text-align: left;
    }
    .planning-contract span::before {
      content: "✓";
      margin-right: .55vw;
      color: var(--green);
      font-weight: 900;
    }
    .process-summary {
      margin-top: 8vh;
      color: var(--muted);
      font-size: 2.15vh;
      font-weight: 750;
      letter-spacing: .04em;
      text-align: center;
    }
    .process-summary strong { color: var(--paper); }
    .process-summary .arrow { margin: 0 1.2vw; color: var(--green); }

    .close-grid {
      display: grid;
      grid-template-columns: 1.2fr .8fr;
      gap: 5vw;
      align-items: center;
      width: min(84vw, 1500px);
    }
    .status-card {
      padding: 3vh;
      border: 1px solid rgba(44, 182, 125, .3);
      border-radius: 20px;
      background: rgba(10, 92, 56, .2);
      text-align: left;
    }
    .status-card strong { display: block; color: var(--green); font-size: 2.7vh; }
    .status-card p { margin-top: 1.1vh; color: #dce7e1; font-size: 2vh; line-height: 1.4; }
    .links { display: grid; gap: 1.8vh; }
    .links a {
      display: block;
      padding: 2.1vh 2vw;
      border: 1px solid rgba(255, 255, 255, .14);
      border-radius: 14px;
      color: var(--paper);
      background: rgba(255, 255, 255, .055);
      font-size: 2vh;
      font-weight: 750;
      text-decoration: none;
      text-align: left;
    }
    .links a:hover, .links a:focus { border-color: var(--green); background: rgba(44, 182, 125, .12); }
    .links small { display: block; margin-top: .5vh; color: var(--muted); font-size: 1.45vh; font-weight: 600; }

    .footer {
      position: fixed;
      right: 3vw;
      bottom: 2.4vh;
      color: #789086;
      font-size: 1.35vh;
      letter-spacing: .08em;
    }
    .progress {
      position: fixed;
      bottom: 0;
      left: 0;
      height: 4px;
      width: 0;
      background: var(--green);
      transition: width .22s ease;
    }

    @media (max-width: 900px) {
      .sail-stop p { font-size: 1.3vh; }
      .feature-hook { font-size: 1.8vh; }
      .process-step h3 { font-size: 1.75vh; }
      .process-step p, .planning-contract { font-size: 1.25vh; }
      .agenda { gap: 1vw; }
      .agenda article { padding: 2vh 1.5vw; }
    }
  </style>
</head>
<body>
  <section class="slide active center" aria-label="Opening slide">
    <div>
      <div class="brand">
        <img src="../drydock_logo.png" alt="Drydock logo">
        <div class="brand-copy">
          <div class="eyebrow">Live project walkthrough</div>
          <h1>Drydock</h1>
          <p class="lead">The SAIL methodology for governed software delivery</p>
        </div>
      </div>
      <div class="sail-route" aria-label="SAIL methodology">
        <div class="sail-stop">
          <div class="sail-letter">S</div>
          <h3>Set Up</h3>
          <p>install · config · init</p>
        </div>
        <div class="sail-stop">
          <div class="sail-letter">A</div>
          <h3>Agile Analyze</h3>
          <p>import · analyze · QuarterDeck · plan</p>
        </div>
        <div class="sail-stop">
          <div class="sail-letter">I</div>
          <h3>Implement</h3>
          <p>build · score · document</p>
        </div>
        <div class="sail-stop">
          <div class="sail-letter">L</div>
          <h3>Loop</h3>
          <p>refit · rebuild · verify</p>
        </div>
      </div>
      <div class="practices" aria-label="Drydock foundations">
        <span>Agile Methodology</span>
        <span class="plus">+</span>
        <span>Test-Driven Development</span>
      </div>
    </div>
  </section>

  <section class="slide" aria-label="Features slide">
    <div class="feature-stage">
      <div class="feature-head">
        <div>
          <div class="eyebrow">Drydock features</div>
          <h2>Existing best practices.<br><span class="accent">Built for specifications.</span></h2>
        </div>
        <div class="practices">
          <span>Agile Methodology</span>
          <span class="plus">+</span>
          <span>Test-Driven Development</span>
        </div>
      </div>
      <div class="constellation" aria-label="Drydock feature hooks">
        <div class="feature-core">
          <strong>DevOps<br>for Specs</strong>
          <span>Full SDLC</span>
        </div>
        <span class="feature-hook hook-best">Existing Best Practices</span>
        <span class="feature-hook hook-simple">Simple and Obvious</span>
        <span class="feature-hook hook-graph">Graph Database</span>
        <span class="feature-hook hook-change">Change Management</span>
        <span class="feature-hook hook-observe">Observability</span>
        <span class="feature-hook hook-govern">Governance</span>
      </div>
    </div>
  </section>

  <section class="slide" aria-label="Agenda slide">
    <div>
      <div class="eyebrow">Today’s route</div>
      <h2>Intro. Walkthrough. Status.</h2>
      <div class="agenda">
        <article>
          <div class="number">1</div>
          <h3>Intro</h3>
          <ul>
            <li>The missing process layer</li>
            <li>Commander, Crew, Blueprint</li>
            <li>Agile + TDD + evidence</li>
          </ul>
        </article>
        <article>
          <div class="number">2</div>
          <h3>Walkthrough</h3>
          <ul>
            <li>Real Target and source material</li>
            <li>QuarterDeck and Manifest</li>
            <li>Build, proof, and Refit</li>
          </ul>
        </article>
        <article>
          <div class="number">3</div>
          <h3>Status</h3>
          <ul>
            <li>Implemented alpha path</li>
            <li>Current boundaries</li>
            <li>Install, inspect, contribute</li>
          </ul>
        </article>
      </div>
    </div>
  </section>

  <section class="slide" aria-label="Drydock process slide">
    <div>
      <div class="eyebrow">The process</div>
      <h2>From Epic to <span class="accent">working software.</span></h2>
      <div class="process-flow" aria-label="Agile and test-driven delivery process">
        <div class="process-step">
          <div class="process-marker">1</div>
          <h3>Epic Refinement</h3>
          <p>Intent, outcomes, boundaries</p>
        </div>
        <div class="process-step">
          <div class="process-marker">2</div>
          <h3>Feature Planning</h3>
          <p>Capabilities and dependencies</p>
        </div>
        <div class="process-step">
          <div class="process-marker">3</div>
          <h3>Story Planning &amp; Estimation</h3>
          <p>Grooming</p>
          <div class="planning-contract">
            <span>Definition of Done</span>
            <span>Acceptance Criteria (AC)</span>
          </div>
        </div>
        <div class="process-step">
          <div class="process-marker">4</div>
          <h3>Sprint Planning</h3>
          <p>Order, grouping, runnable frontier</p>
        </div>
        <div class="process-step">
          <div class="process-marker">5</div>
          <h3>Build Working Software</h3>
          <p>Focused implementation</p>
        </div>
        <div class="process-step">
          <div class="process-marker">6</div>
          <h3>Testing &amp; Review</h3>
          <p>Evidence, acceptance, Commander review</p>
        </div>
      </div>
      <div class="process-summary">
        <strong>Refine</strong><span class="arrow">→</span>
        <strong>Plan</strong><span class="arrow">→</span>
        <strong>Build</strong><span class="arrow">→</span>
        <strong>Verify</strong><span class="arrow">→</span>
        <strong>Repeat</strong>
      </div>
    </div>
  </section>

  <section class="slide center" aria-label="Closing slide">
    <div class="close-grid">
      <div class="brand-copy">
        <div class="eyebrow">Take it for a sail</div>
        <h2>Inspect the process.<br><span class="accent">Build something real.</span></h2>
        <p class="lead muted">Open source · MIT licensed · alpha Commanders welcome</p>
      </div>
      <div>
        <div class="status-card">
          <strong>Drydock 0.1.4 · Alpha</strong>
          <p>Primary SAIL path implemented. Command and Typed Specification contracts remain unstable during the 0.x series.</p>
        </div>
        <div class="links" style="margin-top:2vh">
          <a href="https://webcloudstudio.com/" target="_blank" rel="noreferrer">
            Web Cloud Studio
            <small>webcloudstudio.com</small>
          </a>
          <a href="https://github.com/webcloudstudio/Drydock" target="_blank" rel="noreferrer">
            Drydock on GitHub
            <small>github.com/webcloudstudio/Drydock</small>
          </a>
        </div>
      </div>
    </div>
  </section>

  <div class="footer" id="page">1 / 5</div>
  <div class="progress" id="progress"></div>

  <script>
    const slides = [...document.querySelectorAll(".slide")];
    const page = document.getElementById("page");
    const progress = document.getElementById("progress");
    let current = 0;

    function show(index) {
      current = Math.max(0, Math.min(slides.length - 1, index));
      slides.forEach((slide, number) => slide.classList.toggle("active", number === current));
      page.textContent = `${current + 1} / ${slides.length}`;
      progress.style.width = `${((current + 1) / slides.length) * 100}%`;
      history.replaceState(null, "", `#${current + 1}`);
    }

    document.addEventListener("keydown", (event) => {
      if (["ArrowRight", "PageDown", " "].includes(event.key)) {
        show(current + 1);
        event.preventDefault();
      } else if (["ArrowLeft", "PageUp"].includes(event.key)) {
        show(current - 1);
        event.preventDefault();
      } else if (event.key === "Home") {
        show(0);
      } else if (event.key === "End") {
        show(slides.length - 1);
      } else if (event.key.toLowerCase() === "f") {
        if (document.fullscreenElement) document.exitFullscreen();
        else document.documentElement.requestFullscreen();
      }
    });

    document.addEventListener("click", (event) => {
      if (event.target.closest("a")) return;
      show(event.clientX < window.innerWidth * .25 ? current - 1 : current + 1);
    });

    const requestedSlide = Number.parseInt(location.hash.slice(1), 10);
    show(Number.isNaN(requestedSlide) ? 0 : requestedSlide - 1);
  </script>
</body>
</html>
